@{ Layout = "_Layout"; }

@model HairSalon.Models.Client

<h2>Are you sure you want to delete this client?</h2>
<hr />

@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)
@using (Html.BeginForm())
{
  @Html.HiddenFor(model => model.ClientId)
  <button type="submit" name="id" value="@Model.ClientId">Delete</button><br>
}
<a href='@Url.Action("Index")'><button>Back to List</button></a>&emsp;
<a href='@Url.Action("Index", "Home")'><button>Home</button></a>